# Руководство по использованию двух API провайдеров

## Обзор

Теперь приложение поддерживает два режима работы с AI моделями:

1. **Google Gemini** - прямое подключение к Google Gemini API
2. **NeuroAPI** - OpenAI-совместимый API через https://neuroapi.host

## Настройка

### 1. Выбор провайдера

Откройте **Admin Panel** → раздел **System UI Configuration**

Выберите нужный провайдер в выпадающем списке:
- `Google Gemini` - для работы с официальным Google API
- `NeuroAPI` - для работы через neuroapi.host

⚠️ **Важно:** Настройка провайдера сохраняется на сервере и синхронизируется между устройствами.

### 2. Установка API ключей

В Admin Panel найдите секцию **"API Keys Management"**. Там доступны поля для ввода обоих ключей:

#### Для Google Gemini:
- Введите ключ в поле "🔑 Google Gemini API Key"
- Нажмите кнопку "Save"
- Ключ сохранится в `localStorage.gemini_api_key`

#### Для NeuroAPI:
- Введите ключ в поле "🧠 NeuroAPI Key"
- Нажмите кнопку "Save"
- Ключ сохранится в `localStorage.neuroapi_api_key`

**Альтернативно через консоль браузера:**
```javascript
// Google Gemini
localStorage.setItem('gemini_api_key', 'YOUR_GOOGLE_API_KEY');

// NeuroAPI
localStorage.setItem('neuroapi_api_key', 'YOUR_NEUROAPI_KEY');
```

**Важно:** API ключи хранятся локально в браузере (localStorage) и не отправляются на сервер. Каждый браузер/устройство требует собственной настройки ключей.

### 3. Доступные модели

#### Google Gemini:
- ✨ `gemini-3-pro-preview` - Gemini 3 Pro
- 🖼️ `gemini-3-pro-image-preview` - Gemini 3 Pro Image
- ⚡ `gemini-3-flash-preview` - Gemini 3 Flash
- ⚡ `gemini-2.5-flash` - Gemini 2.5 Flash
- 🖼️ `gemini-2.5-flash-image` - Gemini 2.5 Flash Image

#### NeuroAPI:
- 💬 `gpt-5-mini` - GPT-5 Mini (текст)
- 💬 `gpt-5` - GPT-5 (текст)
- 💬 `gpt-4o` - GPT-4o (текст + vision)
- 💬 `claude-3.5-sonnet` - Claude 3.5 Sonnet (текст)
- 🎨 `gpt-image-1` - GPT Image-1 (генерация изображений)
- 🎨 `dall-e-3` - DALL-E 3 (генерация изображений)

## Различия в API

### Google Gemini API
```typescript
// Использует официальный Google SDK
import { GoogleGenAI } from "@google/genai";

const ai = new GoogleGenAI({ apiKey });
const response = await ai.models.generateContent({
  model: 'gemini-3-pro-preview',
  contents: [{ parts }]
});
```

### NeuroAPI
```typescript
// Использует OpenAI SDK с кастомным baseURL
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey,
  baseURL: 'https://neuroapi.host/v1'
});

// Chat Completion
const response = await client.chat.completions.create({
  model: 'gpt-5-mini',
  messages: [...]
});

// Image Generation
const response = await client.images.generate({
  model: 'gpt-image-1',
  prompt: '...'
});
```

## Особенности работы

### NeuroAPI
- **Генерация изображений**: Модели `gpt-image-1` и `dall-e-3` работают через `/v1/images/generations`
- **Чат и текст**: Все текстовые модели работают через `/v1/chat/completions`
- **Vision модели**: GPT-4o поддерживает работу с изображениями в чате
- **Формат ответа**: Стандартный OpenAI формат

### Google Gemini
- **Генерация изображений**: Модели с суффиксом `-image` генерируют изображения
- **Мультимодальность**: Все модели могут работать с текстом и изображениями
- **Формат ответа**: Google-специфичный формат

## Переключение между провайдерами

Переключение происходит автоматически на основе выбранного провайдера в настройках:

```typescript
// В services/geminiService.ts
export const generateContent = async (...) => {
    const provider = getApiProvider();
    
    if (provider === ApiProvider.NEUROAPI) {
        return await neuroApiService.generateContent(...);
    }
    
    return await generateContentGoogle(...);
};
```

## Проверка настроек

### Через Admin Panel (рекомендуется)
1. Откройте Admin Panel
2. Проверьте секцию "System UI Configuration" - там выбран текущий провайдер
3. В секции "API Keys Management" убедитесь, что нужные ключи введены
4. Кнопка "👁️" позволяет показать/скрыть ключ для проверки

### Через консоль браузера
Откройте консоль (F12):

```javascript
// Проверить текущий провайдер (сохраняется на сервере)
const settings = JSON.parse(localStorage.getItem('wite_ai_system_settings'));
console.log('Current provider:', settings.apiProvider); // 'google' или 'neuroapi'

// Проверить наличие ключей (локально в браузере)
console.log('Google key:', localStorage.getItem('gemini_api_key') ? '✓ Set' : '✗ Not set');
console.log('NeuroAPI key:', localStorage.getItem('neuroapi_api_key') ? '✓ Set' : '✗ Not set');
```

## Устранение неполадок

### Ошибка "API Key is missing"
- Убедитесь, что ключ установлен в localStorage для выбранного провайдера
- Откройте Admin Panel → "API Keys Management" и введите/сохраните ключ
- Проверьте правильность имени ключа: `gemini_api_key` или `neuroapi_api_key`

### Настройки не сохраняются
- **Выбор провайдера**: Сохраняется на сервере через API, синхронизируется между устройствами
- **API ключи**: Сохраняются локально в localStorage браузера, требуют настройки на каждом устройстве

### Ошибка при генерации
1. Проверьте, что выбранная модель соответствует провайдеру
2. Для NeuroAPI: убедитесь, что используете модели из списка NeuroAPI
3. Для Google: убедитесь, что используете модели Gemini
4. Проверьте, что установлен правильный API ключ для выбранного провайдера

### Модель не работает
- Откройте Admin Panel и проверьте настройку API Provider
- Проверьте, что установлен соответствующий API ключ в "API Keys Management"
- Перезагрузите страницу после смены провайдера
- Проверьте консоль браузера на наличие ошибок

## Примеры использования

### Быстрая настройка через Admin Panel
1. Откройте Admin Panel
2. В "System UI Configuration" выберите нужный провайдер
3. В "API Keys Management" введите соответствующий ключ и нажмите "Save"
4. Выберите модель нужного провайдера в интерфейсе генерации

### Установка ключа NeuroAPI через консоль
```javascript
// В консоли браузера (F12)
localStorage.setItem('neuroapi_api_key', 'sk-ваш-ключ-здесь');
console.log('NeuroAPI key установлен!');
```

### Переключение на NeuroAPI
1. Откройте Admin Panel
2. В разделе "System UI Configuration" выберите "NeuroAPI"
3. Убедитесь, что NeuroAPI ключ установлен в "API Keys Management"
4. Выберите одну из моделей NeuroAPI в интерфейсе генерации

### Использование GPT-5 для чата
1. Выберите провайдер: NeuroAPI
2. Выберите модель: GPT-5 Mini (или GPT-5)
3. Используйте Chat Interface как обычно

### Генерация изображений через DALL-E 3
1. Выберите провайдер: NeuroAPI
2. Выберите модель: DALL-E 3
3. Введите промпт на Single Generator или в чате
4. Изображение будет сгенерировано через NeuroAPI

## Техническая информация

### Новые файлы
- `/services/neuroApiService.ts` - Сервис для работы с NeuroAPI
- `API_PROVIDERS.md` - Этот файл с документацией

### Обновленные файлы
- `/types.ts` - Добавлены типы `ApiProvider` и новые модели
- `/services/geminiService.ts` - Добавлен роутинг между провайдерами
- `/services/settingsService.ts` - Добавлены функции для работы с провайдером
- `/constants.ts` - Добавлены новые модели и их параметры
- `/views/AdminPanel.tsx` - Добавлен UI для выбора провайдера

### Зависимости
```json
{
  "openai": "^4.x" // Для работы с NeuroAPI
}
```

## Хранение данных

### Что хранится на сервере
- ✅ Выбор API провайдера (`apiProvider: 'google' | 'neuroapi'`)
- ✅ Все остальные системные настройки (язык, тема, и т.д.)
- ✅ Пользовательские данные, пресеты, история

**Синхронизация:** Настройки автоматически синхронизируются между устройствами через API.

### Что хранится локально (localStorage)
- 🔐 `gemini_api_key` - Google Gemini API ключ
- 🔐 `neuroapi_api_key` - NeuroAPI ключ
- 🔐 Кэш настроек для быстрого доступа

**Важно:** API ключи **НЕ отправляются на сервер** и остаются только в браузере. При использовании нового браузера или устройства необходимо заново ввести ключи.

### Безопасность
- API ключи никогда не покидают ваш браузер
- Ключи используются только для прямых запросов к API провайдерам
- Рекомендуется не делиться скриншотами с видимыми ключами
