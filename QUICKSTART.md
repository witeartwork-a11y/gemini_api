# 🚀 БЫСТРЫЙ СТАРТ

## 3 шага к запуску

### 1. Сборка (2 минуты)
```bash
npm install  # первый раз только
npm run build
```

### 2. Выберите вариант развертывания

#### 🐳 Docker (рекомендуется)
```bash
docker-compose up --build
# Откройте http://localhost
```

#### Apache/Nginx (см. DEPLOYMENT.md)
```bash
sudo cp -r dist/* /var/www/html/
# Дальше следуйте DEPLOYMENT.md
```

#### Локально (для тестирования)
```bash
cd dist && php -S localhost:8080 router.php
# Откройте http://localhost:8080
```

---

## Файлы для изучения

| Файл | Для кого | Размер |
|------|----------|--------|
| **README.md** | Всем | 2 мин |
| **COMPLETE.md** | Обзор | 3 мин |
| **DEPLOYMENT.md** | Для развертывания | 10 мин |
| **api/README.md** | Для разработки API | 5 мин |

---

## Проверка работы

```bash
# API работает?
curl http://localhost:8080/api/history/test-user
# Ожидаемый ответ: []

# Frontend работает?
# Откройте в браузере http://localhost:8080
```

---

## Структура `dist/`

```
dist/  ← Это всё что нужно для развертывания!
├── index.html          # React приложение
├── router.php          # Для локального тестирования
├── .htaccess           # Для Apache
├── api/
│   └── index.php       # PHP API
└── assets/             # JS/CSS
```

---

## Дальше

- Хотите развернуть на сервере? → **DEPLOYMENT.md**
- Хотите понять как работает API? → **api/README.md**  
- Хотите полный обзор? → **COMPLETE.md**

---

**Готово! Приложение полностью работает на PHP.** ✨
