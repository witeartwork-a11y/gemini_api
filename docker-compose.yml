version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./dist:/var/www/html
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html
    restart: always

networks:
  default:
    name: gemini-app
